h2
  span#name
  |  
  span#count.secondary

.form-actions
  .right
    a.btn#refresh(href='#')
      i.icon-refresh
      |  Refresh
    |  
    a.btn#show-queue-query(href='#', 'data-toggle'='#queue-query')
      i.icon-filter
      |  Filter &amp; Sort
  .pagination
  .clear

  #queue-query
    .alert.alert-error
      a.close(data-dismiss='alert') ×
      strong Oh snap!
      |  You should enter a valid JSON string, e.g.
      code {"status": "pending"}
    table
      tr
        td
          | Query:
          textarea#query.formatted
        td
          | Sort:
          textarea#sort.formatted
    p
      a.btn.update-view(href='#') Update View

#entries
