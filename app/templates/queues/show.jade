h2
  span#name
  |  
  span#count.secondary

.control-bar
  .right
    a#refresh(href='#')
      i.icon-refresh
    |  
    a#show-queue-query(href='#', 'data-toggle'='#queue-query')
      i.icon-eye-open
  .clear

  #queue-query
    .alert.alert-error
      a.close(data-dismiss='alert') ×
      strong Oh snap!
      |  You should enter a valid JSON string, e.g.
      code {"status": "pending"}
    table
      tr
        td
          | Query:
          textarea#query.formatted
        td
          | Sort:
          textarea#sort.formatted
    p
      a.btn.update-view(href='#') Update View

#entries
